# Database Configuration
DB_USERNAME=postgres
DB_PASSWORD=your_secure_password_here
DB_NAME=chatbot_db
DB_HOST=db
DB_URL=postgresql://postgres:your_secure_password_here@db:5432/chatbot_db

# JWT Token Secret (change this in production!)
JWT_SECRET=your-very-secure-jwt-secret-key-change-this-in-production

# Frontend URL (used for CORS)
FRONTEND_URL=http://localhost:5173

# Node Environment
NODE_ENV=development

# Google Generative AI (for image analysis)
GOOGLE_API_KEY=AIzaSyxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ===================================================
# N8N WEBHOOK CONFIGURATION
# ===================================================
# Choose ONE option based on your n8n deployment

# OPTION 1: N8N on Sumopod (Production Line) - CURRENT
N8N_WEBHOOK_URL=https://n8n-jn0r4cng3ydi.jkt5.sumopod.my.id/webhook/epson/chat/text

# OPTION 2: N8N on VPS (Alternative)
# Uncomment if n8n is on your own VPS
# N8N_WEBHOOK_URL=https://your-vps-domain.com/webhook/epson/chat/text
# or
# N8N_WEBHOOK_URL=https://your-vps-ip:port/webhook/epson/chat/text

# ===================================================
# PRODUCTION NOTES
# ===================================================
# Before deploying to production:
# 1. Generate a strong JWT_SECRET (use: openssl rand -base64 32)
# 2. Use environment-specific N8N_WEBHOOK_URL
# 3. Set NODE_ENV=production
# 4. Use strong DB_PASSWORD
# 5. Keep all secrets in a secure vault (not in git)
# 6. Configure proper CORS for FRONTEND_URL
